from UI import * #Como la conexion con la API ya esta presente en el archivo de UI, solo es necesario importarlo

ejecutable() #Se hace un llamado a la funcion que actua como interfaz grafica para que el usuario ingrese los datos

